{
  "name": "billmind-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy /billmind requests to GCP Cloud Run",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
