name = "mindnestcorp-subdomain-router"
main = "worker.js"
compatibility_date = "2024-01-01"

# Route configuration for all product subdomains
# After deploying, configure routes in Cloudflare Dashboard
# or uncomment and add routes below:

# routes = [
#   { pattern = "billmind.mindnestcorp.com/*", zone_name = "mindnestcorp.com" },
#   { pattern = "product2.mindnestcorp.com/*", zone_name = "mindnestcorp.com" },
# ]

[vars]
ENVIRONMENT = "production"
